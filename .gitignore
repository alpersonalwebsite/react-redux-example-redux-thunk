# dependencies
/node_modules

/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files. .env was NOT covered, only the .local variants: Create React App
# scaffolds it that way because a committed .env is meant to hold non-secret defaults,
# which is the wrong default for a demo where .env is where a reader puts an API URL.
.env

# secrets and local databases
secrets/
credentials*
*.pem
*.key
*.log
*.sqlite
*.sqlite3
*.db
